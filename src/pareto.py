"""Pareto-front utilities and placeholders."""

from __future__ import annotations

import pandas as pd


def pareto_front(df: pd.DataFrame, objective_columns: list[str]) -> pd.DataFrame:
    """Compute Pareto-optimal rows from objective table (placeholder)."""
    raise NotImplementedError("pareto_front is not implemented yet.")
